<%= form_for(@employee) do |f| %>
  <% if @employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% @employee.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="ui-widget">
  <label for="tags">Manager Name: </label>
  <%= f.text_field :manager_id %>  
</div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
  <%= stylesheet_link_tag 'jquery-ui' %>
    <%= javascript_include_tag 'jquery-1.9.1','
jquery-ui' %>
<script>
  $(function() {
    
    $( "#employee_manager_id" ).autocomplete({
         source: "employee_autocomplete"
    });
  });
  </script>
</head>
<body>
 

 
